@page
@model HairSalon.Web.Pages.Admin.Appointment.CreateAndEdit.SelectEmployedModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>AppointmentDetails</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="InputModel.EmployedDetails.Id" class="control-label"></label>
                <select asp-for="InputModel.EmployedDetails.Id" asp-items="Model.EmploymedListItems" class="form-control">
                    <option>"..."</option>
                </select>
                <span asp-validation-for="InputModel.EmployedDetails.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="../Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}